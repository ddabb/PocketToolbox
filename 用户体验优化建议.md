# 用户体验优化建议

> 基于对各页面与组件代码的通读，从用户视角梳理的体验优化点，按影响程度分档。
> 状态图例：**✅ 已完成** / **🔲 待处理**

## 高优先级（明显影响体验）

### 1. 首屏/进页同步加载大数据，会卡顿或白屏 ✅ 已完成
三处 `aboutToAppear` 都同步读 rawfile 并全量解析：
- `Index.ets:97-154` 首屏同时触发诗词/成语/歇后语三个 VM 的 `loadData/loadIndex`
- `XiehouyuViewModel.ets:50` 读全量 json 后，`buildCategories` 做 **O(N×分类数×关键词数)** 匹配（`XiehouyuViewModel.ets:76-110`），数据量大时首屏明显阻塞
- `IdiomQueryPage.ets:80`、`PoetryQueryPage.ets:32` 同理

**优化说明**：改用「先渲染、后加载」策略，避免首帧阻塞。
- `Index.aboutToAppear` 中将 `loadDailyContent()` 包进 `setTimeout(…, 0)`，首页先渲染标题/功能宫格，每日卡片数据就绪后再填充，消除首屏白屏。
- `IdiomQueryPage` / `PoetryQueryPage` / `XiehouyuQueryPage` 的 `loadData()` 同样延迟到 `setTimeout` 执行；三者 `@State loading` 初始即为 `true`，首帧直接显示加载态，不再卡白屏。
- 后续可进一步把大文件 JSON.parse 放到 `taskpool` 做真正的后台线程解析（当前数据量下 setTimeout 方案已足够）。

### 2. 列表不显示收藏状态 + 收藏页删除无二次确认 ✅ 已完成（确认）
- 歇后语/成语/诗词的所有列表项（搜索结果、分类浏览、按字浏览、接龙结果）都显示"已收藏"♥ 角标。
- `FavoritesPage.removeItem` 删除走 `showDialog` 二次确认。

### 3. 分享只有"存相册"，没有系统分享面板 ✅ 已完成（确认）
`ShareImageUtil.shareImageViaPanel` 已接入 **Share Kit（`systemShare.ShareController.show`）**，详情弹窗与 Md2Png 均提供"分享"按钮拉起系统分享面板。

## 中优先级（一致性/反馈）

### 4. 搜索历史仅成语页有，其余没有 ✅ 已完成
- 成语页原有 `idiom_query_history`（`IdiomQueryPage`）。
- 新增 `xiehouyu_query_history`（`XiehouyuQueryPage`，搜索框下方横向历史标签，点击回填）与 `poetry_query_history`（`PoetryQueryPage`，未搜索时展示，点击回填）。
- 三处实现一致：preferences 存储、最多保留 5 条、去重并以最近优先排序。

### 5. 硬编码颜色未随深色模式自适应 ✅ 已完成
- 将语义色改为 `$r('app.color.*')` 资源引用：`Index` 收藏红心、`XiehouyuQueryPage` 猜谜成败色（`success_green`/`error_red`）、`FavoritesPage` 确认弹窗按钮色（`tertiary_text`/`danger_red`）。
- `color.json`（base + dark）已具备对应资源；`EntryAbility` 当前强制浅色，一旦放开深色模式，上述色可自动适配。
- 说明：分享卡片/生成图的强调色（如 `#c0392b`/`#e67e22`/`#fdf6ec`）属于"生成图片"的设计色，不参与 App 主题，保持硬编码；按钮上的 `#ffffff` 文字在深浅模式下均可读，保持不变。

### 6. Toast 时长/文案不统一 ✅ 已完成
- 统一基准：**操作/成功反馈 1000ms，错误 2000ms**。
- 消除原先 1000/1500/2000 混用：复制、收藏、已保存相册、未找到结果等均为 1000ms；保存失败、分享失败、生成失败等均为 2000ms。

## 低优先级（细节增强）

### 7. 歇后语猜谜无连续激励/自动下一题 ✅ 已完成
- `XiehouyuQueryPage` 答对后显示"答对了！连对 N 题"连对计数（≥2 题时提示）。
- 答对后 1s 自动切换到下一题，并通过 `TextInputController.requestFocus()` 聚焦答案输入框，减少手动操作。

### 8. 收藏上限 200 无提示 ✅ 已完成
- `FavoritesUtil.add` 改为返回 `FavoriteAddResult { added, truncated }`；当超过 200 条静默截断最早一条时，调用处弹出 Toast：**"收藏已达上限，最早一条已移除"**（2000ms）。
- 已覆盖四个收藏入口：`DetailModal`、`PoemContentPage`、`XiehouyuQueryPage`（列表与详情弹窗）。

### 9. 字号用 `vp` 而非 `fp`，不随系统字体缩放 ✅ 已完成（验证无需改动）
- 核查结论：ArkUI 的 `.fontSize(n)` **默认单位即为 `fp`**，会随系统字体大小设置缩放；搜索 `vp` 命中项均为 `NavPathStack` 子串误匹配，代码中并无显式 `vp` 字号。
- 正文字号（如 12–16fp）均已随系统字体缩放，无障碍场景（老年/弱视力用户）无需额外修改。

### 10. 首页"每日一诗"点击只进查询页 ✅ 已完成
- `Index.DailyPoemCard` 点击在已加载当日诗时直接 `pushPathByName('PoemContentPage', dailyPoemItem)` 跳转当日诗详情；未加载完成则回退到 `PoetryQueryPage`。减少层级跳转。

### 11. Md2Png 切换模板后旧生成图未失效提示 ✅ 已完成
- `Md2PngPage` Swiper `onChange` 切换模板时，立即清空 `generatedImageUri` 并 Toast 提示 **"模板已更换，请重新生成"**，避免旧图与当前模板混淆。

---

## 进度汇总

| 编号 | 优化点 | 状态 |
| --- | --- | --- |
| 1 | 首屏/进页异步加载，避免白屏卡顿 | ✅ 已完成 |
| 2 | 列表收藏标记 + 删除二次确认 | ✅ 已完成（确认） |
| 3 | 接入系统分享面板（Share Kit） | ✅ 已完成（确认） |
| 4 | 三方搜索历史统一补齐 | ✅ 已完成 |
| 5 | 硬编码语义色抽到 color.json 资源 | ✅ 已完成 |
| 6 | 统一 Toast 时长（1000/2000） | ✅ 已完成 |
| 7 | 歇后语猜谜连对激励 + 自动下一题 | ✅ 已完成 |
| 8 | 收藏上限 200 提示 | ✅ 已完成 |
| 9 | 字号随系统字体缩放（fp） | ✅ 已完成（验证无需改动） |
| 10 | 每日一诗直达当日诗详情 | ✅ 已完成 |
| 11 | Md2Png 切模板清除旧图并提示 | ✅ 已完成 |

> 如需改动，建议优先处理 1 / 2 / 3（均已处理）。
