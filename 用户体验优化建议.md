# 用户体验优化建议

> 基于对各页面与组件代码的通读，从用户视角梳理的体验优化点，按影响程度分档。

## 高优先级（明显影响体验）

### 1. 首屏/进页同步加载大数据，会卡顿或白屏
三处 `aboutToAppear` 都同步读 rawfile 并全量解析：
- `Index.ets:97-154` 首屏同时触发诗词/成语/歇后语三个 VM 的 `loadData/loadIndex`
- `XiehouyuViewModel.ets:50` 读全量 json 后，`buildCategories` 做 **O(N×分类数×关键词数)** 匹配（`XiehouyuViewModel.ets:76-110`），数据量大时首屏明显阻塞
- `IdiomQueryPage.ets:80`、`PoetryQueryPage.ets:32` 同理

建议：用 `TaskPool`/`setTimeout` 异步化，或改为"用到哪个功能才加载哪个"的懒加载，分类匹配可预建索引。

### 2. 列表不显示收藏状态 + 收藏页删除无二次确认
- 歇后语/成语/诗词的所有列表项（搜索结果、分类浏览、按字浏览、接龙结果）都不显示"已收藏"标记（如 `XiehouyuQueryPage.ets:383-413`、`IdiomQueryPage.ets:466-506`、`PoetryQueryPage.ets:320-348`），用户收藏后回列表无法区分。
- `FavoritesPage.ets:138` 的 ✕ 直接 `removeItem`，**无任何确认**，极易误删。

建议：列表项加收藏心形角标；删除走确认弹窗（或左滑+确认）。

### 3. 分享只有"存相册"，没有系统分享面板
`ShareImageUtil.saveImageToAlbum` 和 `Md2PngPage.saveToAlbum` 都只调 `showAssetsCreationDialog` 存到相册。用户想直接发微信/微博，得先存图再手动去分享，链路长。

建议：接入 **Share Kit（`showSharePanel`）**，一键拉起系统分享面板。

## 中优先级（一致性/反馈）

### 4. 搜索历史仅成语页有，其余没有
`IdiomQueryPage.ets:107-140` 实现了查询历史（存 preferences），但歇后语、诗词搜索都没有，体验割裂。

建议：三处统一补齐，或统一去掉。

### 5. 硬编码颜色未随深色模式自适应
多处写死：`'#ffffff'`（按钮文字）、`fontColor(... ? '#2d7d46' : '#e74c3c')`（成败绿红，`XiehouyuQueryPage.ets:688`）、分享强调色 `'#c0392b'/'#e67e22'` 等。当前因 `EntryAbility` 强制浅色暂看不出，但一旦改为适配深色（审核需要的改动），这些色在深色背景上对比度会出问题。

建议：统一抽到 `color.json`（base + dark）。

### 6. Toast 时长/文案不统一
复制 1000ms、报错 1500/2000ms、无结果 1500ms 混用。

建议：统一基准（如操作反馈 1000ms、错误 2000ms）。

## 低优先级（细节增强）

### 7. 歇后语猜谜无连续激励/自动下一题
`XiehouyuQueryPage.ets:196-219`：提交后只显示对错文案，不自动聚焦下一题、无连对反馈。可加"答对后高亮下一题"或连对计数。

### 8. 收藏上限 200 无提示
`FavoritesUtil.ets:98-100` 静默截断，用户无感知。建议达上限时 Toast 提示。

### 9. 字号用 `vp` 而非 `fp`，不随系统字体缩放
不利无障碍（老年/弱视力用户）。建议正文类改用 `fp`。

### 10. 首页"每日一诗"点击只进查询页
`Index.ets:559` 点击跳 `PoetryQueryPage`，没直达当日诗详情。建议直跳当日诗，减少层级。

### 11. Md2Png 切换模板后旧生成图未失效提示
`Md2PngPage.ets:352` 切模板只更新 `currentTemplateIndex`，已生成的 `generatedImageUri` 仍是旧图，用户易混淆。建议切模板即清除旧图或提示"模板已换，请重新生成"。

---

如需改动，建议优先处理 1 / 2 / 3。
