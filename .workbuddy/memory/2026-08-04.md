# 2026-08-04

## 项目分析：PocketToolbox（鸿蒙 App，显示名"解语珠玑"）
- 路径 F:\PocketToolbox。HarmonyOS 原生应用（ArkTS/ArkUI），SDK 5.1.0(18)，hvigor 构建，单 entry 模块。
- 包名 com.ddabb.toolbox，vendor lgayy，versionName 1.0.4。
- 定位：国学文字（诗词/成语/歇后语 + 朗读 TTS/收藏/每日签到/每日挑战/成就）+ 实用工具（进制换算/单位换算/时区/GCD-LCM/质因数分解/词云/Md2Png/数独求解）。
- 数据全本地化（resources/rawfile 下的 JSON），无后端网络请求；唯一三方依赖 @luvi/lv-markdown-in。
- 技术亮点：数独 DLX（Dancing Links）求解引擎；双引擎 OCR（CoreVisionKit 系统 OCR + 自研像素网格识别 SudokuPixel，15 张测试图覆盖多字体/颜色/尺寸）；自研词云布局（WordCloudTask + BitmapPlacer 位图碰撞放置）。
- 分层：pages / viewmodel(单例) / model / components / core(sudoku|wordcloud|layout|renderer|style) / common(utils|constants)。
- 已知风险：build-profile.json5 明文含签名密钥密码（敏感）；Index.ets 为 ~1500 行超大单体组件（首页/工具/我的三大 tab 全在一个 struct）；无自动化测试（devDeps 有 hypium/hamock 但未用）。
