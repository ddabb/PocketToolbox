# 2026-08-05 工作记录

## photos.jpg 数独 OCR 修复（SudokuPixel.ets）
- **根因**：棋盘定位 bbox 一直找错。 photos.jpg（2160×3840 竖图，数独印在白纸上、放在浅木纹桌面、顶部有阴影）=等比缩放到 253×450；真实棋盘偏在**左上方**（约 `[13,131,234,353]`），而之前代码返回的 `[16,132,233,407]` 抓的是木板/阴影。不是 cell 识别问题。
- **修复方案**：新增 `findBoxLongRun`（长行程 run-length 过滤定位棋盘）——对严格阈值二值图做水平/垂直长行程过滤并排除贴图像边缘的行程（避免边缘木纹/暗角），保留的像素包围盒即棋盘。写入 `entry/src/main/ets/core/sudoku/SudokuPixel.ets`，并在 `findBoardBBox` 第一行优先调用（同时修掉了 region search 退化 bbox `{0,0,...}` 抢先 return 的 bug）。
- **验证**：Node 复刻流水线 `scripts/test_pipeline.js` 与 `.ets` 流水线逐行一致，photos.jpg 结果 **RAW 60%（18/30）/ REFINED 70%（21/30），均 > 30% 阈值（minAcc）PASS**。`.ets` 版 `abs` 阈值对浅底 bg≈180 算得 =80，与 Node 绝对阈值 80 等价。
- **待用户真实验证**：在 DevEco 跑 `entry/src/ohosTest/ets/test/SudokuOcr.test.ets`（hvigor 构建需 SDK/模拟器，沙箱跑不了）。
- **遗留**：`.ets` 里的 `findBBoxConservative` 仍停留在旧版（未与 replica 同步），但现在是 long-run 之后的 fallback，对 photos.jpg 走不到，不影响。
- Node 脚本（test_pipeline.js / replica_full.js / locate_*.js / diag_*.js / ascii_view.js）均为本地验证工具，非 App 组成。

## scripts/ 清理（16:55）
- 按用户要求删除 scripts/ 内无用过程产物：diag_*、locate_*、test_bbox*、test_margin*、test_faithful*、ascii_view*、ocr_*、diagnose_*、replica_full.js 及全部 .png 诊断图（共 33 个）。
- 保留：generate_sudoku.js、generate_sudoku_variants.js、sudoku_puzzle.html（项目原有）、test_pipeline.js（最终验证脚本，可回归验证）。
- 删除经 safe-delete 拦截，文件进入回收站（可回收），scripts/ 当前仅 4 个文件。
